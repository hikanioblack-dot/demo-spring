# Автосервис — Задание 3

## 📌 Тема
Сервис для управления автосервисом: приём заказов на ремонт, назначение механиков, отслеживание статусов выполнения работ.

## 🧩 Основные сущности
1. **Клиент (Client)**
    - Имя, телефон (уникальный), email (уникальный)
2. **Автомобиль (Car)**
    - Марка, модель, год выпуска, гос. номер (уникальный), привязан к клиенту
3. **Механик (Mechanic)**
    - Имя, специализация, телефон (уникальный)
4. **Заказ (Order)**
    - Описание, статус (`принято`, `в работе`, `готово`, `выдано`), дата создания, привязан к автомобилю и клиенту
5. **Назначение (Assignment)**
    - Связь заказа с механиком, статус выполнения

## ⚙️ Операции

### 🔁 CRUD (Create, Read, Update, Delete)
Для всех 5 сущностей реализован полный набор:
- Создание (`POST`)
- Получение списка и по ID (`GET`)
- Обновление (`PUT`)
- Удаление (`DELETE`)

### 🚀 Бизнес-операции (5 шт.)
1. **Оформление нового заказа**  
   `POST /business/orders` — создаёт заказ с привязкой к автомобилю и клиенту.
2. **Назначение механика на заказ**  
   `POST /business/assign` — связывает заказ и механика.
3. **Завершение заказа**  
   `POST /business/orders/{id}/complete` — меняет статус на «готово» и помечает все назначения как выполненные.
4. **Получение заказов клиента с информацией об автомобиле**  
   `GET /business/clients/{id}/orders` — возвращает заказы + данные авто.
5. **Получение загрузки механика**  
   `GET /business/mechanics/{id}/workload` — показывает количество активных заказов.

## 🗃 База данных
- Используется **PostgreSQL**
- Все таблицы связаны внешними ключами (`FOREIGN KEY`)
- Добавлены ограничения:
    - `UNIQUE` на телефон, email, гос. номер
    - `CHECK` на год выпуска и статус заказа
- Тестовые данные загружаются автоматически при старте

## ▶️ Запуск
1. Установите PostgreSQL и создайте БД:
   ```sql
   CREATE DATABASE autoservice;
   CREATE USER autoservice_user WITH PASSWORD 'secret';
   GRANT ALL PRIVILEGES ON DATABASE autoservice TO autoservice_user;
   ALTER USER autoservice_user CREATEDB;